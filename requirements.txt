# Core acquisition and analysis
numpy>=2.2.3
scipy>=1.15.1
matplotlib>=3.10.0
pandas>=2.2.3

# RTL-SDR control. Needs the librtlsdr system library:
#   sudo apt install rtl-sdr librtlsdr-dev
#
# The upper bound is load-bearing. pyrtlsdr 0.4.0+ binds
# rtlsdr_set_dithering unconditionally at import, and that symbol exists
# only in the rtl-sdr-blog fork of librtlsdr -- not in the mainline
# Osmocom build that Debian and Ubuntu ship. Against the packaged library
# it fails on import with:
#
#   AttributeError: /usr/lib/.../librtlsdr.so: undefined symbol:
#   rtlsdr_set_dithering
#
# See acquisition/README.md for the blog-fork route if you want 0.5.0.
pyrtlsdr>=0.3.0,<0.4

# pyrtlsdr 0.3.0 imports pkg_resources, which setuptools 81 removed.
setuptools<81

# Web viewer in legacy/sdr-viewer/
flask>=3.0.0
